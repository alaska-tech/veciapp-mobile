import { View, Text } from 'react-native';

export default function VendorHome() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>Welcome cachón 0</Text>
    </View>
  );
}